# Base Configuration
baseUrl=https://www.saucedemo.com/
browser=chrome

# Execution Mode
# Options: auto, true, false
# auto = detects CI environment automatically
# true = always run headless
# false = always run headed
headless=auto

# Browser Options
browserTimeout=30000
slowMo=0